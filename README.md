# Bash gerelateerde files

* bash_profile
* vimrc

## Pre Commit Scripts

Een script om de syntax te checken (momenteel alleen PHP) en merge errors te detecteren:

- chk_syntax.sh
- run_checks.sh

Activeren middels:

    $ ln -s ../../chk_syntax.sh .git/hooks/pre-commit



